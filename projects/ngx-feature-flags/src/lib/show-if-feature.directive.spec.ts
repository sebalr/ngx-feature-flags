import { ShowIfFeatureDirective } from './show-if-feature.directive';

describe('ShowIfFeatureDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowIfFeatureDirective();
    expect(directive).toBeTruthy();
  });
});
